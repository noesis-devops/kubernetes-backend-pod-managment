apiVersion: v1
kind: Service
metadata:
  name: selenium-hub-{{ port }}-service
spec:
  selector:
    app: selenium-hub-{{ port }}
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4444
      nodePort: {{ port }}
      name: hub
  type: NodePort